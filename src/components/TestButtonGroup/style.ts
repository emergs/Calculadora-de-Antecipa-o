import styled from "styled-components";

const TestButtonGroupStyle = styled.div`
  display: flex;
  justify-content: space-around;

  button{
    font:var(--span);
    width: 100%;
    height: 50px;
    margin: 0 10px;
    margin-top: 32px;
  }
`

export default TestButtonGroupStyle