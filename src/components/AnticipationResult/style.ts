import styled from "styled-components";

const ListGroup = styled.ul`
  width: 100%;
`

const ListItem = styled.li`
  width: 100%;
`

const Description = styled.span`
  font-style: italic;
`

const DiscountValue = styled.span`
  font-weight: 700;
`

export { ListGroup, ListItem, Description, DiscountValue }